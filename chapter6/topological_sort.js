var execute = require('../GetInput.js').execute;

execute(function(input) {
    input = input[0].split(' ');
    main();
});

/**
 * @input m two-tuples (u, v) contains n variables, which represent u less than v
 * @output asc order
 */

function main() {
}
